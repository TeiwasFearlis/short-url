server.port=8080
#spring.datasource.url=${db.url}
#spring.datasource.username=${db.username}
#spring.datasource.password=${db.password}
#spring.datasource.platform=postgresql
#spring.r2dbc.url=${db.url}
#spring.r2dbc.username=${db.username}
#spring.r2dbc.password=${db.password}
#spring.r2dbc.name=postgresql
spring.liquibase.change-log=classpath:db/changelog/db.changelog-master.xml
spring.liquibase.default-schema=${db.schema}
spring.liquibase.enabled=true
spring.liquibase.url=jdbc:postgresql://${db.host}/${db.database}
spring.liquibase.user=${db.username}
spring.liquibase.password=${db.password}

context.initializer.classes=ru.test.shorturl.AppInitializer

# used by async service code
spring.r2dbc.username=${db.username}
spring.r2dbc.password=${db.password}
spring.r2dbc.url=r2dbc:postgresql://${db.host}/${db.database}
spring.r2dbc.pool.max-size=10
spring.r2dbc.properties.sslMode=REQUIRE

#spring.cache.cache-names=url
#spring.cache.caffeine.spec=maximumSize=10000,expireAfterAccess=600s
spring.cache.caffeine.spec=maximumSize=250,expireAfterWrite=15m
caffeine.specs.url=maximumSize=1000,expireAfterWrite=1h
spring.cache.type=caffeine


hostName=localhost:8080/go/